import { IntervalleDistance } from '../models/intervalle-distance';

describe('IntervalleDistance', () => {
  it('should create an instance', () => {
    expect(new IntervalleDistance()).toBeTruthy();
  });
});
